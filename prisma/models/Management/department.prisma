model Department {
    id    Int     @id @default(autoincrement())
    title String  @unique
    url   String
    icon  String?

    business_unit           BusinessUnit[]
    business_unit_process   BusinessUnitProcess[]
    user_department_sector  UserDepartmentSector[]

    process_item            ProcessItem[]

    sector Sector[]    
}
