export interface CreatePeopleData {
  name: string;
  birth_date: string;
  status: string;
  registration_number?: string;
  photo?: string;
  sex?: string;
  hore_date?: string;
  termination_date?: string;
}
